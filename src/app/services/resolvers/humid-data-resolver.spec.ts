import { HumidDataResolver } from './humid-data-resolver';

describe('HumidDataResolver', () => {
  it('should create an instance', () => {
    expect(new HumidDataResolver()).toBeTruthy();
  });
});
